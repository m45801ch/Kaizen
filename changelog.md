# Changelog

所有重要的變更都會記錄在此檔案。

## [V3.5] - 2026-08-08

### 新增
- 改善前／後「下方說明」新增**獨立說明卡片**（內層框線），與上方圖片、外框清晰區隔。
- AI 生成時**重點詞彙強調**：AI 以「**」標記重點詞（如滑倒、墜落、風險），前端以**紅色粗體**顯示。
- 標題（如具體現象／根本成因／潛在風險等）依類型**自動著色**（藍／橘／紫／紅／綠／青／琥珀）。
- 標題前圖示改為 **SVG 圖示**（取自 allsvgicons.com Tabler 單色系列）：現象＝放大鏡、成因＝齒輪、範圍＝地球、風險＝警告三角、對策＝扳手、重點＝圖釘、效果＝勾選；圖示統一單一灰階色，視覺更簡潔。
- 改善前／後卡片改為**紅框／綠框**。

### 修正
- 列印（PDF）時說明卡片與上方圖片間隔預留（照片區上下左右內距），不再重疊或貼框。
- 左側「複製／清除」按鈕同列對齊。

## [V3.4] - 2026-08-08

### 新增
- 新增 **OpenRouter** 與 **Groq** 兩家 AI 供應商，供應商切換改為下拉選單（共 4 家：Google Gemini / OpenAI / OpenRouter / Groq）。
  - OpenRouter：金鑰於 openrouter.ai/settings/keys 取得，支援跨廠商模型。
  - Groq：金鑰於 console.groq.com/keys 取得，高速推理。
- 左側「API 設定」與「圖片壓縮」面板改為**預設收合**，點標題展開，節省版面空間。
- 新增「**一鍵清除全部**」按鈕：一次清空改善主題、改善前／後（左右兩側）、預期效益、文件標題、照片與最近編輯記憶，避免殘留舊主題。

### 修正
- 修正二次使用時 AI 抓到舊主題的問題：改以「最近編輯的一側」作為分析來源，避免右側殘留內容蓋過新輸入。
- 修正左側「改善前／改善後」的「複製」與「清除」按鈕未對齊同一列的問題。

## [V3.3] - 2026-08-04

### 新增
- 新增「**圖片壓縮**」調整面板：
  - 可開關「上傳時壓縮圖片」，預設開啟，存成 JPEG（品質 0.85）。
  - 可調整最大邊長（400–4000px，預設 1600px），列印與 PDF 依此解析度輸出。
- PDF 檔名自動帶入「改善主題」內容（例如：`遷移攝影機位置以提升維修安全.pdf`）。

### 其他
- 加入 `.gitignore`，僅追蹤 `index.html`，忽略 `logo.jpg` 與壓縮檔。

## [V3.2] - 2026-08-02

### 新增
- 新增「**更換 LOGO**」功能：可上傳自訂 LOGO（自動壓縮至寬度 360px 內並記憶），可還原為內建 LOGO，列印／PDF 同步顯示。

## [V3.0] - 2026-08-02

### 新增
- 改善提案生成器（KAIZEN V3 · AI 版）正式推出。
- 支援 Google Gemini 與 OpenAI 兩家 AI 服務，API Key 輸入後自動載入可用模型。
- AI 一鍵生成正式表格內容與正式措辭描述。
- 改善前／後照片上傳（每邊最多 10 張、單張 15MB）、A4 直向／橫向切換、自訂文件標題、彩色分析列點預覽、預期效益三欄。
- 所有資料僅存於瀏覽器（localStorage + IndexedDB），列印／匯出 A4 PDF。
